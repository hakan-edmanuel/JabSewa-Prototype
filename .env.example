# ============================================================================
# JabSewa — Environment Variables (MODE MOCK / OFFLINE)
# ============================================================================
# Aplikasi berjalan 100% lokal: data & auth tersimpan di LocalStorage browser
# (src/lib/storage.js). TIDAK ADA variabel Supabase yang dibutuhkan lagi —
# `npm run dev` langsung jalan tanpa konfigurasi apa pun.
#
# File ini sengaja dikosongkan. Jika suatu saat backend (mis. Supabase)
# dipasang kembali, tambahkan variabel yang diperlukan di sini.
# ============================================================================
